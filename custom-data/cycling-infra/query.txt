[out:json];

(
  area["name"="Magyarország"]->.boundaryarea;
  
  // get cycleways
  way[highway=cycleway](area.boundaryarea);
  way[bicycle=designated](area.boundaryarea);
  way[cycleway=track](area.boundaryarea);

  // get lanes
  way[cycleway=lane](area.boundaryarea);
  way["cycleway:right"=lane](area.boundaryarea);
  way["cycleway:left"=lane](area.boundaryarea);
  
  // other
  way[bicycle=permissive](area.boundaryarea);
  way[bicycle=yes][highway](area.boundaryarea);
  
  way[cycleway=share_busway](area.boundaryarea);
  way["cycleway:right"=share_busway](area.boundaryarea);
  way["cycleway:left"=share_busway](area.boundaryarea);
  
  way[cycleway=shared_lane](area.boundaryarea);
  way["cycleway:right"=shared_lane](area.boundaryarea);
  way["cycleway:left"=shared_lane](area.boundaryarea);
);

out body;
>;
out skel qt;