{
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --detailed-report"
  },
  "browserslist": "> 5%, last 3 versions, not dead, not op_mini all, not kaios > 0, not and_uc > 0",
  "dependencies": {
    "@mapbox-controls/inspect": "^3.0.0",
    "@mapbox-controls/styles": "^3.0.0",
    "@mapbox/mapbox-gl-geocoder": "^5.0.3",
    "mapbox-gl": "^3.9.4"
  },
  "devDependencies": {
    "@types/mapbox__mapbox-gl-geocoder": "^5.0.0",
    "@types/node": "^22.0.0",
    "parcel": "^2.7.0"
  },
  "engines": {
    "node": "22.x"
  }
}
